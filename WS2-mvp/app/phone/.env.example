# LiveKit (server-side only — used by /api/token)
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_ROOM=jio-test

# Vite (exposed to browser — VITE_ prefix required)
VITE_LIVEKIT_URL=wss://jiobuddy-y3inkf8x.livekit.cloud
VITE_TOKEN_URL=/api/token
